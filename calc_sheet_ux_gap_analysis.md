# 计算表（calc_sheet）对标 Excel 的可用性 / 易用性 / 便利性差距分析 + 改进意见

> 角色：产品经理（许清楚）　|　对象：lawfirm_app 分成计算 → 内嵌类 Excel 计算表
> 方法：已真实通读 `calc_engine_spec.md`、`calc_sheet_view.py`、`calc_sheet.py`、`calc_eval.py`、`calc_dialogs.py`、`calc_formula.py`
> 定位：在 v1（spec 已冻结）基础上，挑出**真正影响"对标 Excel 编辑体验"**的高价值差距，给出可评审的分级与意见，不堆砌。

---

## 0. 现状速写（已落地、不再重复建议）

公式栏 + 自动补全、查看/编辑双模式、Ctrl+V 粘贴 TSV 值、选中区域统计、Ctrl+滚轮缩放（按表记忆）、左栏折叠 + 最后编辑人/时间提示、DATA/PARAM/自定义指标选择器、导出 Excel（带/不带公式）、损坏表锁定只读 + 库外备份、公式格悬停显示原文、错误值红色。

**关键约束（影响所有方案取舍）：**
- 存储 = `calc_sheet.content` 单 JSON 字段，整张网格；单元格 key 为 `"r,c"`（0 基），`{raw, kind}`；**公式以原文文本存，引用是 A1 字符串**。
- 引擎公式内核 `calc_formula.py` 已有 `Parser(src).parse()` 产出 AST（`CellRef`/`RangeRef`/`FuncCall`…），但**没有 AST→文本的反向序列化器**；且**绝对引用 `$` 当前不被 tokenizer/parser 支持**（`_TOKEN_RE` 的 op 集不含 `$`，`a1_to_rc` 也不认 `$`）。
- 编辑即存：每次单元格提交经 `_recalc_fill → cs.save_content` 立即落库（刷新 `updated_by/updated_at`）。
- DB 在 Seafile 同步范围：**跨机同时编辑 = 后写覆盖**；已用"最后编辑人/时间"缓解，但无法阻止。
- 视觉沿用 D-Notion clean，新增交互不得破坏。
- 键盘：`keyPressEvent` 里**只拦截了 Ctrl+V**；编辑触发用 `AnyKeyPressed`（任意键直接覆盖进入编辑）。其余导航键无显式处理。

---

## 1. 对标 Excel 差距清单（用户价值 × 实现成本二维）

> 价值/成本取 高 / 中 / 低。优先级：P0 = 廉价且必备（日常高频摩擦）、P1 = 高价值、中等成本（本阶段重点）、P2 = 锦上添花 / 待定场景。

| # | 差距 | 用户价值 | 实现成本 | 优先级 | 为什么 Excel 用户会期待它 | 当前有无 workaround |
|---|------|---------|---------|-------|--------------------------|--------------------|
| G1 | **撤销 / 重做** | 高 | 中 | P1 | 误操作（手滑改错一格、误删一片）是表格最高频事故；Excel 用户 Ctrl+Z 是肌肉记忆 | 无。改错只能手动改回；损坏表有只读保护，但普通误改无任何回退 |
| G2 | **插入 / 删除行、列（带引用重写）** | 高 | 中 | P1 | 增删一个职工 / 一列月份是常态；当前只能尾部追加，中间插行会把后续公式全打乱 | 极弱。只能重建表或手工改所有引用；spec §11.5 明确本期不做 |
| G3 | **键盘导航补全**（Tab 右移 / Enter 下移 / F2 编辑 / Esc 取消 / Home·End / Ctrl+方向跳边界 / Del 清空 / Ctrl+C·X） | 高 | 低~中 | P1（其中 F2/Esc/Del/Ctrl+C·X 子集≈P0） | 鼠标流在密集表格里极慢；Excel 纯键盘可秒级操作 | 仅方向键移动、Ctrl+V、双击编辑；F2/Esc/Tab/Enter 行为不准、无复制/剪切/清空 |
| G4 | **复制公式块 + 填充柄（fill handle，相对引用自动调整）** | 高 | 中 | P1 | "对 12 个月各填一遍 `=DATA(...)*提成比例`" 靠拖拽秒完；当前粘贴只带值、不带公式 | 无。只能逐格用"插入数据引用"弹窗；公式块复制完全不支持 |
| G5 | **多 sheet 标签页 UI** | 中 | 中~高 | P2（待定） | 一个"汇总表"引用多张分表是经典用法；引擎已支持 `Sheet2!A1` 跨表引用 | 引擎层可用**按表名**跨引（如 `=Sheet2!A1`），但无任何 UI 切换/新建/重命名 sheet 的入口 |
| G6 | **查找 / 替换** | 中 | 低~中 | P2 | 在大表里定位"某职工""某指标引用"必不可少 | 无 |
| G7 | **每格独立数字格式**（货币 / 百分比 / 小数位） | 中 | 中 | P2 | 律所核算天然需要"元""%""保留两位小数"差异化呈现 | 全局统一两位小数 + 千分位（`_fmt`）；`spec §11.4` 明确不做 |
| G8 | **冻结窗格**（冻结首列 / 首 N 行） | 低~中 | 低~中 | P2 | 宽表横向滚动时看不见"职工名"列、纵向滚动时看不见表头行 | 仅 QTableWidget 自带表头常驻，无"冻结首列/首行" |
| G9 | **列头 / 行头自定义标签**（schema 已有 `col_headers`/`row_headers`，但 UI 从未渲染） | 低~中 | 低 | P1（顺手补） | 用户给列起名（如"开票""分成"）比 A/B/C 直观 | **零**。数据模型已预留字段，`_fill` 却硬编码 `_col_name(i)` 与 `str(r+1)` |
| G10 | **重命名表 UI**（`rename_sheet` 引擎已实现，但界面无按钮） | 中 | 低 | P1（顺手补） | 复制出的"副本"要改名是刚需 | 复制后只能删了重建；引擎有函数但无入口 |

> 说明：spec §11.5 已列"撤销/重做、行列增删、每格格式、多 sheet tab、fill handle、复制公式块、查找替换、键盘导航"为不做项——它们正是上表 G1~G8 的主体，与代码实况一致。G9/G10 是**代码已留能力但 UI 没接线**的隐藏缺口，成本低、体验收益实在，单独点出。

---

## 2. 针对性改进意见（按差距给交互细节，不泛泛而谈）

### 2.1 撤销 / 重做（G1）
- **是否跨"编辑即存"做**：建议做**会话内（in-session）命令栈**，不清空前不持久化版本。理由：落库是即时的，"保存/未保存"边界本就不存在；做"跨保存"的全量历史既无必要也放大 Seafile 覆盖风险。Undo = 把该命令的逆操作写回 `content` 再 `save_content`（与现有保存同路径）。
- **命令栈粒度**：
  - 单格编辑（公式栏回车 / 单元格提交）= 1 条命令（存该格 `before/after` 的 raw）；
  - 批量粘贴（TSV / 公式块）= 1 条命令（存受影响 cell-key 集合的 `before/after` + `rows/cols` 维度变化，便于整片撤销）；
  - 参数改动、增删行列 = 各 1 条。
  - 存**差分**而非整表快照（content JSON 对个人表虽小，但差分更省、撤销更准）。
- **与 Seafile 覆盖共存**：撤销只回滚**本机本次会话**对该表的操作，无法感知另一台机器的并发编辑——这点要在"撤销" tooltip / 帮助里明示（与现有的"后保存者覆盖"提示口径一致）。不要在 undo 里引入跨机版本合并（过度设计）。
- **键位**：Ctrl+Z 撤销 / Ctrl+Y（或 Ctrl+Shift+Z）重做。在 `keyPressEvent` 拦截，注意别和全局字号占用冲突（缩放已用 Ctrl+滚轮，撤销用 Ctrl+Z 无冲突）。

### 2.2 插入 / 删除行、列（G2）—— 引擎已基本就绪，关键在"引用重写"
当前 spec 禁止的根因是"插入后 A1 引用不会自动重写"。但 `calc_formula` 已有 AST，重写**可行**，三种策略：

- **(a) 真重写 A1 引用（推荐目标态）**：对受影响的每个公式 `parse()`→ 平移 `CellRef`/`RangeRef` 坐标 → **重新渲染为文本**写回 raw。
  - 必须补两件事：① 新增 **AST→文本序列化器**（当前只有 parser，没有 formatter，这是核心成本）；② 先支持**绝对引用 `$`**（见 2.4），否则插入会把本应固定的 `$A$1` 也平移，反而错。
  - 平移规则（Excel 语义，务必一致）：插入点在引用**之前**→不动；插入点**落在区域引用内部**→区域应**扩张**（如 `SUM(A2:A50)` 在第 10 行插行→`SUM(A2:A51)`，而非平移）；相对引用按轴平移；绝对引用不平移。
  - 同时**整表 cell-key 平移**：所有 `"r,c"` 中 row≥k 的 key 整体 +1（删则 −1），这是纯机械映射，安全。
- **(b) 插入后标红冲突格、用户手改**：只平移 cell-key（安全），但公式 raw 不动→引用错位→重算可能出错；把受影响公式格标红/#REF 提示，逼用户改。成本最低但**严重违背"对标 Excel"**，仅作 (a) 落地前的临时兜底，不建议长期保留。
- **(c) 维持现状只尾部加**：即当前行为，差距最大，不推荐。

> 推荐：**先落地 `$` 绝对引用（2.4），再上 (a) 真重写**。插入/删除行、列是分成表高频操作（加一个职工、加一列月份），P1 内 ROI 高。

### 2.3 多 sheet 标签页（G5）
- **引擎现状**：`calc_eval` 已能按表名跨引（`has_sheet`/`_sheet_name`/`Sheet2!A1` 全通），`calc_sheet.workbook_id` 列已预埋（恒为 1），但**没有 `calc_workbook` 表、没有 tab 控件、左侧"表列表"是跨所有表的扁平列表**。
- **UI 缺口**：① 没有底部 sheet 标签条（新建/切换/重命名/删除 sheet）；② 左侧列表当前既当"工作簿列表"又当"sheet 列表"，引入 tab 后定位要重构；③ `rename_sheet` 引擎函数**已写好但界面没按钮**（顺手补，见 G10）。
- **本期是否值得做**：**建议 P2 暂缓**，除非出现真实"汇总表引用多张分表"场景。原因：本应用每张核算表相对独立，"跨表"目前靠"在公式里写另一张表的名字"即可（虽丑但能用），且做 tab 必然牵动左侧导航重构（成本中~高）。**但**：若决定做，务必同步把"按表名跨引"升级为"tab 内 sheet 引用"，并复用已实现的 `rename_sheet`。

### 2.4 复制公式块 + 填充柄（G4）—— 相对引用自动调整
- **当前硬伤**：① `paste_tsv` 只从剪贴板读 TSV **值**，且把内容 `classify_cell` 后按普通格存，**公式不被携带**；② 没有 Ctrl+C 复制单元格的动作（剪贴板里只有显示值或空）；③ **绝对引用 `$` 未实现**，所以"相对/绝对"区分在引擎层根本不存在，fill handle 调不了。
- **实现要点**：
  1. **先支持 `$` 绝对引用**：在 tokenizer 把 `$` 作为引用前缀 token、parser 在 `CellRef` 上记 `abs_row/abs_col` 标志、`a1_to_rc` 兼容 `$A$1 / A$1 / $A1`。这是 G2/G4 的共同前置，单列一次成本。
  2. **块复制**：新增 Ctrl+C（复制选中区），从 `content.cells` 取** raw 原文**而非显示值，写入剪贴板（可沿用 TSV 但每格带 `=` 公式原文）。Ctrl+V 时**识别"源是应用内公式块"→ 复制 raw 而非值**。
  3. **相对引用调整**：复制/填充时，对目标区每个公式 `parse()`→ 把相对 `CellRef`/`RangeRef` 按"源→目标位移"平移（绝对部分不动）→ 序列化回文本。规则与 Excel 一致（如源 `=B2*C2` 右移一列→`=C2*D2`；`=$B2` 列固定、行相对）。
  4. **填充柄 UI**：在选中区右下角画一个小方块手柄，拖拽方向决定填充方向；序列识别（1,2,3 / 1月,2月）本期可不做，先支持"复制式填充"（与 Excel 拖同值/同公式一致）。
- **风险**：序列化器必须与 parser 严格互逆（尤其保留 `$`、区域、跨表 `Sheet!`、字符串里的形似 A1 文本）；先单测 `parse→shift→render→parse` 闭环再接 UI。

### 2.5 键盘导航（G3）—— 最廉价的 P0/P1 高价值项
`keyPressEvent` 当前只拦 Ctrl+V，且编辑触发用 `AnyKeyPressed`（任意键直接覆盖）。建议补一张**最小必备键位表**：

| 键 | Excel 行为 | 当前状态 | 处理建议 |
|----|-----------|---------|---------|
| 方向键 | 移动选择 | QTableWidget 默认已有 | 保留 |
| Tab / Shift+Tab | 右/左移并提交 | 默认行为不准（与 AnyKeyPressed 冲突） | 显式：非编辑态→移动；编辑态→提交并移动 |
| Enter / Shift+Enter | 下/上移并提交 | 默认进编辑或停留 | 显式：提交并下移（匹配 Excel） |
| F2 | 就地编辑（不覆盖） | **缺失**（直接打字会覆盖） | 新增：进入编辑且光标置末 |
| Esc | 取消编辑、还原 | **缺失**（离开即提交） | 新增：丢弃本次编辑 |
| Home / End | 行首 / 行尾 | **缺失** | 新增 |
| Ctrl+方向 | 跳数据边界 | **缺失** | 新增（按当前表有值范围跳） |
| Shift+方向 | 扩展选区 | 需验证 | 验证并补 |
| Delete / Backspace | 清空单元格（不进编辑） | **缺失**（会被 AnyKeyPressed 当成输入） | 新增：清空 raw |
| Ctrl+C / Ctrl+X | 复制 / 剪切（带公式） | **缺失** | 新增（支撑 G4） |
| Ctrl+Z / Ctrl+Y | 撤销 / 重做 | **缺失** | 接 G1 |

> 注意：`AnyKeyPressed` 编辑触发会让"输入即覆盖"，这与 F2 理念冲突——建议编辑模式改为 `DoubleClicked | EditKeyPressed | F2` 触发，普通打字覆盖改为"仅在单元格已选中且按字母/数字时覆盖"（保留 Excel  overwrite 手感），但务必保留 F2 就地编辑与 Esc 取消。

### 2.6 查找 / 替换（G6）、每格格式（G7）、冻结窗格（G8）
- **查找/替换（P2）**：低~中成本。先做"查找"（在 raw + 计算值里搜，定位并高亮/跳格），替换放二期。对"哪些格引用了某职工 / 某指标"很有用（呼应 spec §10 提到的"单元格级反查本期不做"，查找可部分缓解）。
- **每格独立格式（P2）**：在 `content.cells[key]` 加可选 `fmt` 字段（`{type:'currency'|'percent'|'num', dec:2}`），`_fmt` 按 fmt 渲染；粘贴 / 导出 Excel 同步映射为 openpyxl 数字格式。**先确认业务是否真需要**——全局两位小数+千分位对律所多数场景已够，差异化格式是加分项非刚需。
- **冻结窗格（P2）**：QTableWidget 无原生冻结列/行，需自绘或用 `setSpan`/叠层技巧。价值取决于表是否常宽/常长；可先支持"冻结首列"（职工名列）这一最实用单项。

### 2.7 两项"已留能力未接线"的顺手补（G9 / G10，低成本高体感）
- **列头/行头标签（G9）**：`default_content` 已有 `col_headers`/`row_headers`，但 `_fill` 硬编码 `_col_name(i)` 与 `str(r+1)`。补一个"编辑表头"入口（双击表头或工具栏按钮），把内容写进 `content` 并渲染——比 A/B/C 直观得多，成本极低。
- **重命名表（G10）**：`rename_sheet` 引擎函数已就绪，界面只需在左栏加"重命名"右键/按钮并接 `QInputDialog` + 校验（复用 `validate_sheet_name`）。复制出"副本"后改名是刚需。

---

## 3. 待用户确认的 Open Questions（需开发者拍板）

1. **撤销是否只做"会话内"？** 我建议不做跨保存/跨机历史（避免放大 Seafile 覆盖风险）。是否同意？还是希望有"最近 N 步"甚至"版本"概念？
2. **插入/删除行列本期是否做 (a) 真重写？** 若做，需先排期"绝对引用 `$` 支持"（G2/G4 共用前置）。还是本期先用 (b) 标红兜底？
3. **多 sheet 标签页本期是否投入？** 我倾向 P2 暂缓（除非有"汇总表引用分表"的真实场景）。是否已有此类需求？
4. **相对/绝对引用（`$`）是否本期实现？** 它是插入重写与 fill handle 的共同前提；即便暂不做插入，若做 fill handle 也必须先有 `$`。
5. **块复制 / fill handle 本期是否做？** 当前粘贴只带值；若要做"复制公式块 + 拖拽填充"，需新增 Ctrl+C 与应用内公式复制协议。优先级是否高于撤销/插入？
6. **每格独立数字格式是否本期要？** 还是维持全局两位小数+千分位（spec §11.4 口径）即可？
7. **键盘导航的 `AnyKeyPressed` 覆盖手感是否保留？** 若保留，F2 就地编辑 + Esc 取消需与之并存；若改为"选中态打字覆盖、F2 进编辑"，请确认。
8. **表头标签 / 重命名 是否一并顺手补？** 两者引擎/数据已就绪，仅差 UI 接线，成本低、体感实。

---

## 4. 一句话总结（给评审）

> 当前 v1 在"公式能力 / 数据引用 / 导出 / 防护"上已很 Excel 化；**真正拉低"编辑体验"的是交互层四件事：无撤销、不能插行列、不能复制公式/拖填充、键盘导航不全**。其中键盘导航与"表头/重命名接线"成本最低、体感最强，建议**优先 P0/P1 顺手做掉**；插入行列与复制公式块是 P1 主力，二者都依赖先补"AST 序列化 + 绝对引用 `$`"这一共同底座；多 sheet tab 与每格格式、冻结窗格、查找替换建议 P2 视真实场景再定。

# T6 go/no-go 评审包（Pilot 收口）

- 日期：2026-09-11
- 仓库：`C:\Users\Bingo\Desktop\buddy2\lawfirm_app`（分支 `pilot/dotnet`）
- 依据：`docs\csharp-wpf-refactor-plan-2026-09-09.md` §6 任务分解 —— T6 = 三件验证实测 + go/no-go 评审

## 一、三件验证状态

| # | 验证项 | 硬门槛 | 当前结论 | 证据 |
|---|--------|--------|----------|------|
| ① | **NPOI 列名 100% 一致** | 与 Python golden 逐格一致 | ✅ **PASS**（硬门槛达成） | `docs\t6-npoi-column-check.md`：42 文件 / 80 sheet / **15 列逐字相等**，7 维 **0 差异**，23,055 坐标逐格 0 差异 |
| ② | **3 台 PC 3 天 0 锁冲突** | 零数据库锁冲突 | 🟡 **静态核查 PASS，实测待跑** | `docs\t6-lock-conflict-check.md`：全仓 0 处 DML/DDL、0 处 `ExecuteNonQuery`、唯一打开模式 `ReadOnly`、连接全为 `using` 短生命周期；风险 高3/中4/低2 |
| ③ | **用户主观 ≥ 4/5** | 用户打分 | ⏳ **待你打分**（表见下） | — |

### ① 附带发现（不推翻 PASS，但需你定夺）

| 编号 | 问题 | 状态 |
|------|------|------|
| **DIFF-1** | 个人结算总表**表头行丢了「加粗 + 灰底 F2F2F2」** | ✅ **已修并验证**（commit `1755624`）：新增 `headerText`（bold + `F2F2F2` 实心底）用于第 2 行；抽查 5 份 xlsx，`bold=True / fill=F2F2F2` |
| **DIFF-2** | 月度结算表「空名单兜底 sheet」header/footer 页边距 0.3（应为 0.5） | ✅ **已修并验证**（同上 commit）：补写 `Header/Footer = 0.5`，实测 `0.75/0.75/1/1/0.5/0.5` |
| **GAP-1** | 月度结算表真实列名未被现有产物覆盖 | ✅ **已补齐并验证**（2026-09-12）：用户跑 `t2_2025.bat` 产出 73 sheet 真实报表 → **官方 `diff_xlsx.py` 判定 PASS（列名/合并/数字格式/页面设置/逐格 全绿）** |

### ② 进入 3 天实测前**必须**先做（否则 Day1 就会撞假故障）

| 动作 | 为什么 |
|------|--------|
| 1. `data\lawfirm.db` 备份到 Seafile 目录**之外** | 历史事故：Seafile 曾清空目录 |
| 2. 三台机器 `setx LAWFIRM_DB ""` 并重启 | 残留该环境变量会**静默**读错库，被误判为同步问题 |
| 3. 把库合并为单一文件（清掉 `-wal`/`-shm`） | WAL 伴生文件 + Seafile 同步时序 = 头号风险（打不开库 / 库损坏 / 读到旧数据） |
| 4. Seafile 忽略 `csharp/*/bin`、`csharp/*/obj` | 实际最大的冲突源（build churn），不是数据库问题 |
| 5. 三台各自导出到 **Seafile 之外**的目录 | 同名 `个人结算总表_张三.xlsx` 必冲突 |

> 完整 8 步准备动作、每日 10 步操作、报错关键词判读、记录表 → 见 `docs\t6-lock-conflict-check.md` 第三节。

### ② 配套可观测性改造（R-M4 / R-M2，2026-09-12 已落地）

> 目的：3 天实测里最容易出现的**假故障**是「读到的不是我以为的那个库」。
> 改造后无需猜，界面与命令行都能一眼确认。

| 编号 | 原风险 | 处置 | 验证 |
|------|--------|------|------|
| **R-M4** | `FindDatabase()` 兜底路径算成 `bin\data\lawfirm.db`（必然不存在）且**不做存在性校验**，把「找不到」伪装成正常路径 | ①逐级向上查到盘符根；②每个候选做 **File.Exists + SQLite 文件头**双重校验（挡 Seafile 半截/0 字节文件）；③全部落空则抛 `FileNotFoundException`，**列出逐条尝试过的候选** | 仓库外运行 → 打印 6 条 `[跳过] …（文件不存在）` 并 exit 1，不再返回假路径 |
| **R-M2** | 残留 `LAWFIRM_DB` 会**静默**把程序指到旧副本 | ①侧栏底部常驻「数据库：lawfirm.db」，找不到标红；②悬浮看完整路径 + 来源 + 完整查找过程；③点击复制路径；④窗口标题带数据库绝对路径；⑤残留变量被忽略时显式标注「已设置但不可用，本次已忽略」 | `--diag-shell` 冒烟通过：`IsOk=True / Display=数据库：lawfirm.db / Title=律所台账 — …\data\lawfirm.db` |
| 附带 | 库定位失败会在建页阶段把整个窗口带崩 | `PersonalSettlementViewModel.InitializeOnce()` 加 try/catch，只写日志不崩窗 | 代码路径已覆盖 |

新增诊断命令（三机报障时先跑这两个）：

```
dotnet run --project csharp\LawFirm.Cli -- --diag-db       # 数据库定位链路（env/来源/路径/逐条查找过程），找不到 exit 1
dotnet run --project csharp\LawFirm.Cli -- --diag-shell    # 主窗 XAML 冒烟（不弹窗，纯构造 + 关窗）
```

**回归**：改动后跑 T2 真实数据闸门（2025-12，73 sheet），官方 `diff_xlsx.py` 判定 **PASS（7 维全绿，RC=0）** —— 数据层改动未影响任何导出数值。

---

## 二、③ 用户主观评分表（≥4/5 为通过）

请按 1–5 分打分（5 = 与 Python 版持平或更好）：

| 维度 | 说明 | 分数 |
|------|------|------|
| 1. 界面还原度 | 侧栏/标题/表格/间距，是否像 Python 版（Notion 风格） | ___ |
| 2. 操作流畅度 | 切换页面、选人、刷新有无卡顿；导出时窗口是否可拖动 | ___ |
| 3. 数据正确性 | 看到的数字与 Python 版对得上（抽查 2–3 人） | ___ |
| 4. 导出可用性 | 导出的 xlsx 能直接拿去用（列名、格式、加粗灰底） | ___ |
| 5. 稳定性 | 有没有报错、闪退、卡死 | ___ |
| **总评（取平均）** | **须 ≥ 4.0** | ___ |

补充意见：______________________________________________

---

## 三、go/no-go 判据与当前结论

| 条件 | 状态 |
|------|------|
| ① 列名 100% + 7 维 0 差异 | ✅ 达成（**官方 diff_xlsx.py 复核**：个人结算总表 42/42 PASS；月度结算表 73 sheet PASS，GAP-1 已闭） |
| DIFF-1 / DIFF-2 | ✅ 已修并验证（commit `1755624`） |
| ② 3 天 0 锁冲突 | 🟡 静态达标，实测待跑 |
| ③ 用户主观 ≥ 4/5 | ⏳ 待打分 |
| GAP-1 月度结算表真实列名 | ✅ 已补验通过（`t2_2025.bat` + 官方 diff） |

**当前建议：CONDITIONAL GO（有条件推进）**
代码层已完成可交付状态（只读纪律、导出一致性、UI 功能均通过），建议顺序：
1. ~~修 DIFF-1/DIFF-2~~ ✅ 已完成
2. 跑 `scripts\t2_2025.bat` 补齐 GAP-1（真实月度结算表列名）
3. 填完上表评分
4. 做②的准备动作 8 条 → 跑 3 天实测 → 记录表填完
5. 三件全绿 → 正式 **GO**，Pilot 收口

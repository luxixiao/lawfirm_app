__pycache__/
*.pyc
.venv/
# 运行时数据(Seafile同步, 不进git)
data/
# 写通道备份快照(S1)：绝不进 git（其中可能含整库业务数据）
data/backups/
# 个人结算总表导出
个人结算总表_*.xlsx
# UI 无头冒烟截图产物
tests/_smoke_*.png
# PyInstaller 构建产物（exe 由本机 build_exe.bat 生成，不进 git）
build/
dist/
build_log.txt
# C# / .NET 构建产物（dotnet build 生成，不进 git）
bin/
obj/
*.user
.vs/
# T2 验证流水线产出的 C# 导出 xlsx（golden 在 tests/golden，产物不进 git）
csharp/out/
# 本地一次性验证/自检产物（脚本运行日志 + 自检报告，不进 git）
*.log
scripts/self_diff_report.txt

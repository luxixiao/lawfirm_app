# 发票台账页（方案 D：原始镜表 + 同步）UI 规格说明书

> 读者：UI 工程师（先据此出设计稿）；实现：AI 工程师（设计稿确认后落地）
> 关联需求：台账数据页面重构 2.4（新建「发票台账」页），并联动 1 / 2.1 / 2.2 / 2.3 / 2.5

---

## 0. 一句话目标

新建一个独立的「发票台账」页：
- 以 **`raw_invoice` 原始镜表** 展示发票台账 Excel 中**所有 sheet 的逐行原始数据**（所见即 Excel 原文，不做归一化）；
- 支持对原始数据**增 / 删 / 改**；
- 提供「**同步到 invoice**」按钮，把原始行解析后写入/更新计算用的归一化 `invoice` 表；
- 所有对发票台账的修改，按 **2.5** 只记入 `change_log`（且仅记发票台账）。

---

## 1. 导航与页面位置（重构联动）

重构后「数据」组导航变化（来源：2.2 / 2.3 / 2.4）：

| 改造项 | 动作 | 说明 |
|--------|------|------|
| 2.4 新建「发票台账」页 | **新增**到「数据」组 | 本规格描述的对象；原「台账数据」页的「发票」Tab 移除 |
| 2.3 取消「发票 / 收款」Tab | 删除「台账数据」页的 发票、收款 Tab | 发票查看/编辑统一收归新「发票台账」页 |
| 2.2 删除「员工」Tab | 删除「台账数据」页的 员工 Tab | 员工维护统一在「维护」组已有的「员工管理」页（两者都操作 `staff` 表，重复） |
| 2.1 费用归类→维护 | 「费用归类」Tab 从「台账数据」移到「维护」组（新建或并入「员工管理」同类维护页） | 见 §13 |
| 2.5 修改记录只记发票台账 | 「台账数据」页的「修改记录」Tab 保留，但 `change_log` 此后**只**写入发票台账相关修改 | 见 §10 |

> 注：本规格只详述「发票台账」页（方案 D）。2.1/2.2/2.3 的其余页面删改由 AI 工程师在落地阶段一并处理，UI 工程师只需保证「发票台账」页与新的导航结构协调（出现在「数据」组、顺序建议：导入 / 发票台账 / 导入记录 / 台账数据）。

---

## 2. 数据模型：`raw_invoice` 原始镜表

### 2.1 建表 DDL（实现阶段由 AI 工程师落地，这里给 UI 工程师看字段含义）

```sql
CREATE TABLE IF NOT EXISTS raw_invoice (
    id               INTEGER PRIMARY KEY AUTOINCREMENT,
    src_file         TEXT,                       -- 导入文件名
    src_sheet        TEXT,                       -- 来源 sheet 名（多 sheet 区分）
    src_row          INTEGER,                    -- 在 Excel 中的行号（含标题/表头，1 基）
    seq              TEXT,                       -- 序号（原表序号列，存文本）
    invoice_no       TEXT,                       -- 发票号码
    kind             TEXT,                       -- 发票种类
    invoice_date_raw TEXT,                       -- 开票日期（原样存文本，不归一化）
    status           TEXT,                       -- 发票状态
    voucher_no       TEXT,                       -- 凭证号
    goods            TEXT,                       -- 货物、应税劳务及服务
    buyer            TEXT,                       -- 购方名称
    total_amount_raw TEXT,                       -- 价税合计（原样存文本，可含负号）
    net_amount_raw   TEXT,                       -- 不含税金额
    tax_rate_raw     TEXT,                       -- 税率
    tax_raw          TEXT,                       -- 税额
    remark           TEXT,                       -- 备注
    orig_invoice_no  TEXT,                       -- 解析出的"被红冲蓝字数电票号码"
    is_red           INTEGER DEFAULT 0,          -- 1=红字(价税合计为负) 0=蓝字（辅助显示）
    synced           INTEGER DEFAULT 0,          -- 0=未同步到 invoice 1=已同步
    created_at       TEXT DEFAULT (datetime('now','localtime')),
    updated_at       TEXT
);
```

### 2.2 关键设计原则（方案 D 的核心）

- **raw_invoice = Excel 原文的 1:1 镜像**：数值列（价税合计/不含税/税率/税额）一律**存原始文本**，不做类型转换、不做归一化。归一化（日期解析成 `YYYY-MM-DD`、金额转 float、红字关联解析）只在「同步到 invoice」时发生。
- **invoice = 计算用归一化表**（已有，PK=发票号码），两者通过 `invoice_no` 关联。
- **导入时双写入**：原始落 `raw_invoice`（`synced=1`），归一化落 `invoice`；所以导入完成后两份天然对齐。

---

## 3. 导入流程变化（给 AI 工程师实现、UI 工程师了解）

- `parse_invoice_file` 改为**遍历 Excel 所有 sheet**（`pd.ExcelFile(path).sheet_names`），逐 sheet 逐行：
  - 写入 `raw_invoice`：原始文本列 + `src_file`/`src_sheet`/`src_row` + 解析出的 `orig_invoice_no`/`is_red`（`total<0`）。
  - 同时按现有逻辑（归一化、`normalize_date`、float 金额、红冲校验）写入 `invoice`。
- 同文件内 `invoice_no` 重复仍报错（保持现有校验）；跨 sheet 同号视为同一张票，raw 保留多行溯源、invoice 按 `invoice_no` upsert（后者覆盖前者，并在 `change_log` 留痕迹）。

---

## 4. 页面整体布局（请 UI 工程师据此出设计稿）

```
┌──────────────────────────────────────────────────────────────┐
│  发票台账                                        [SubtitleLabel] │
│  Caption: 查看并增删改发票台账全部原始数据（多 sheet），可同步到计算表 │
├──────────────────────────────────────────────────────────────┤
│  来源sheet: [全部 ▼]   搜索: [_________] [🔍]   状态: [全部▼]    │  ← 顶部筛选条
├──────────────────────────────────────────────────────────────┤
│  发票号码 | 发票种类 | 开票日期 | 状态 | 凭证号 | 购方名称 | ... | 同步 │  ← 主表格(可横滚)
│  ...17 列...                                                          │
├──────────────────────────────────────────────────────────────┤
│ [+ 新增] [删除所选] [编辑所选] [⇄ 同步到invoice] [⟳ 刷新]     左侧工具栏 │
│                                                  右侧: 已选 N 行 / 待同步 M 行 │
├──────────────────────────────────────────────────────────────┤
│  操作日志（只读，最近操作摘要）                                      │
└──────────────────────────────────────────────────────────────┘
```

风格要求：沿用现有 qfluentwidgets / Notion-like 视觉基线（与「发票收款情况」「手动补录」等页一致），深色/浅色皮肤均可。表格列多，**允许横向滚动**，列宽手动调整后通过既有 `column_state` 机制持久化（关闭/切页不复位）。

---

## 5. 主表格列定义（展示 `raw_invoice`）

| 列序 | 字段 | 来源列 | 备注 |
|------|------|--------|------|
| 1 | 来源sheet | src_sheet | 多 sheet 区分；可默认隐藏、放 tooltip |
| 2 | 行号 | src_row | Excel 内行号，便于溯源 |
| 3 | 序号 | seq | 原表序号 |
| 4 | 发票号码 | invoice_no | 红字可标红显示 |
| 5 | 发票种类 | kind | |
| 6 | 开票日期 | invoice_date_raw | 原始文本（如 `2025/1/3`） |
| 7 | 发票状态 | status | |
| 8 | 凭证号 | voucher_no | |
| 9 | 购方名称 | buyer | |
| 10 | 价税合计 | total_amount_raw | 负数红字；右对齐 |
| 11 | 不含税金额 | net_amount_raw | 右对齐 |
| 12 | 税率 | tax_rate_raw | |
| 13 | 税额 | tax_raw | 右对齐 |
| 14 | 货物、应税劳务及服务 | goods | 内容长，允许省略号+tooltip |
| 15 | 备注 | remark | 含红冲关联；长内容 tooltip |
| 16 | 同步状态 | synced | 显示「已同步」/「待同步」徽标（绿/橙） |

> 列宽默认自适应（无省略号优先，过长列才省略并加 tooltip），用户可手动拖拽，拖后记录。

---

## 6. 工具栏操作与行为

| 操作 | 触发 | 行为 |
|------|------|------|
| **+ 新增** | 按钮 | 弹出「新增发票行」对话框（见 §7），所有原始字段留空；保存后写入 `raw_invoice`（`synced=0`、`is_red` 由金额正负算），并记 `change_log`（table=raw_invoice, field="新增"）。 |
| **删除所选** | 按钮 | 删除选中 1..N 行；若某行 `synced=1`，弹确认「是否同步删除计算表中的对应发票？」→ 是则从 `invoice` 一并删除（按 `invoice_no`）并记日志；否仅删 raw。 |
| **编辑所选** | 按钮 / 双击行 | 弹出「编辑发票行」对话框（见 §7），覆盖该行全部原始字段；保存即写 `raw_invoice` 并把 `synced` 置 0（标记脏），逐字段差异记 `change_log`。 |
| **⇄ 同步到 invoice** | 按钮 | 把当前筛选/选中范围内的**待同步（synced=0）**行，逐行解析归一化后 upsert 进 `invoice`；成功后 `synced=1`。建议：默认同步「全部待同步行」，或支持「仅同步选中行」。同步失败（如日期/金额解析失败）的行留在待同步并提示。 |
| **搜索** | 输入框 | 按 发票号码 / 购方名称 / 备注 模糊匹配（沿用现有 `invoice_rows` 的 keyword 逻辑思路），实时过滤。 |
| **来源sheet 筛选** | 下拉 | 列出 `raw_invoice` 中 distinct `src_sheet` + 「全部」；切换即按 sheet 过滤。 |
| **状态 筛选** | 下拉 | 全部 / 已同步 / 待同步。 |
| **⟳ 刷新** | 按钮 | 重新从 DB 加载。 |

---

## 7. 编辑 / 新增 对话框字段

对话框标题：`新增发票行` 或 `编辑发票：{invoice_no}`。字段（与 raw_invoice 一一对应，均为文本输入，保持"原始"语义）：

- 发票号码（必填，红字可为负号码体系，但号码本身文本）
- 发票种类
- 开票日期（原始文本，提示：格式如 2025/1/3，同步时归一化）
- 发票状态
- 凭证号
- 货物、应税劳务及服务（多行文本框）
- 购方名称
- 价税合计（文本，可带负号表示红字）
- 不含税金额
- 税率
- 税额
- 备注（多行，红冲关联信息在此）
- 来源sheet（新增时可手填或默认"手动补录"；编辑时只读显示）

底部：修改备注输入框（`note`，写入 `change_log.note`）+ 确定/取消。

> 备选（供 UI 工程师定）：对"原始镜表"也可支持**单元格双击直接行内编辑**（失焦即存 raw 并置 synced=0），体验更贴近 Excel。若采用行内编辑，仍需保留「编辑所选」对话框作为全字段入口。

---

## 8. 同步逻辑与状态标识（关键）

- `raw_invoice.synced`：
  - `1` 已同步：import 写入或上次同步成功。
  - `0` 待同步：手动新增 / 编辑 / 删除未同步 / 导入异常残留。
- 表格「同步状态」列用**彩色徽标**：已同步=绿、待同步=橙；顶部状态栏显示「待同步 M 行」提示。
- **归一化规则**（同步时，复用现有 `parse_invoice_file` 的解析函数）：
  - `invoice_date_raw` → `normalize_date` → `invoice.invoice_date`（YYYY-MM-DD）
  - `total_amount_raw` → float（去逗号）→ `invoice.total_amount`（负=红字）
  - `net_amount_raw`/`tax_raw` → float → `invoice.net_amount`/`tax`
  - `remark` 中正则提取 `被红冲蓝字数电票号码：(\d+)` → `invoice.orig_invoice_no`
  - 红冲校验：原票金额 + 对应红字合计 ≥ 0（沿用现有约束，失败时该行保持待同步并提示）
- upsert 目标 `invoice` 以 `invoice_no` 为键；若发票号冲突，后者覆盖前者（并记 change_log）。

---

## 9. 修改记录（需求 2.5）

> **2026-09-02 修订**（导入复核页改造，详见 `design/import_review_spec.md` §12）
> 本节原文以「发票台账」页 + `raw_invoice` 描述，与落地实现及后续演进已不一致，现更正如下。

### 9.1 更正：镜表与页面的对应关系

| 页面 | 镜表 | 说明 |
|---|---|---|
| **销项发票**页（`invoice_ledger_view`） | `raw_invoice` | 销项发票文档镜像；**无编辑功能** |
| **发票台账**页（`invoice_ledger_doc_view`） | `raw_ledger` | 发票台账文档镜像；**曾**提供右键「编辑此行」 |

即：本节原文所称 `raw_invoice` 的编辑能力，实际落在了 `raw_ledger` / 发票台账页上。

### 9.2 更正：`change_log` 的写入方

原文「`change_log` 此后**只**由『发票台账』页写入」已失效。现规则：

- **写入方 = 「数据导入 → 导入复核」页**（新增，合并原「导入确认」与「导入校验」），覆盖两条路径：
  - **导入前**：确认页的修正/编辑，在点「确认入库」时统一落盘
  - **导入后**：回写（`app/engine/review_writeback.apply_edit`）
- **操作对象 = `raw_ledger`**（发票台账文档镜表），同步影响 `invoice` / `charge_detail` / `collection`
- **发票台账页的编辑功能将移除**（改造阶段 5），改为顶部提示引导至「导入复核」页；其右键「查看修改记录」保留
- 其他页面（收款 / 费用 / 员工等）的编辑不写 `change_log`（不变）

### 9.3 记录规范

沿用原范式，并补充分层（详见 `design/import_review_spec.md` §12）：

- 编辑镜表行 → 每个变化的原始字段一行：`table_name='raw_ledger'`, `record_id=<镜表行 id>`, `field=列名`, `old_value/new_value`
- 导入时修正问题行（无旧值）→ `field='(导入修正)'`，`old_value=''`，`new_value=<填入内容>`
- 同步落地 → 1 条汇总：`field='(同步)'`，`new_value` 汇总业务表变化（**不逐条记，避免行数爆炸**）
- **`note`（修改原因）必填**
- 「修改表名」列显示 `friendly_table`：导入时修正写 `发票台账 · 导入修正`；回写沿用 `build_friendly_table()`（如 `发票台账2025-01已开票已入账`）

### 9.4 「修改记录」页展示

- 展示 `change_log` 全量，列：修改时间 / 修改表名 / 发票号码 / 对方 / 金额 / 经办人 / 旧值 / 新值 / 备注
- **改造项**：补「字段」列（`field`，置于「修改表名」后）—— 原 9 列不含 `field`，导致只看旧值/新值无法判断改的是哪个字段；并给 `field` 以 `(` 开头的行（`(导入修正)`/`(同步)`/`(新增)`/`(删除)`）加淡色底纹分层

---

## 10. 列宽持久化（复用既有机制）

- 主表格接入 `attach_persistence(table, "invoice_ledger", "raw")`（复用 `app/ui/column_state.py`）。
- 渲染时 `auto_fit_then_restore`：默认自适应无省略号；用户拖过列宽后记录，关闭/切页不复位。
- 「新增/编辑」对话框不要求持久化。

---

## 11. 校验与边界

- 发票号码为空不允许新增/同步（同步时若 `invoice_no` 空 → 保持待同步并提示）。
- 同步时金额/日期解析失败 → 该行保持待同步，弹窗汇总失败清单（不中断其余行）。
- 删除已同步行时级联删 `invoice` 需二次确认（防误删计算数据）。
- 红字发票（`is_red=1`）在表格中**红色**显示价税合计；同步仍走正常 upsert。
- 多 sheet 同名 sheet 不会出现（Excel 约束），无需处理。
- 大数据量（数百~上千行）：表格用 `setHorizontalScrollMode(ScrollPerPixel)` + 分页或虚拟滚动建议（视 UI 工程师方案），保证流畅。

---

## 12. 与 2.1 / 2.2 / 2.3 的联动清单（AI 工程师落地时一并处理）

| 项 | 动作 | 状态 |
|----|------|------|
| 2.1 | 「费用归类」Tab 从「台账数据」移到「维护」组（新建「费用类型维护」页或并入现有维护页）；`expense_cat` 表不变 | 待实现 |
| 2.2 | 删除「台账数据」页「员工」Tab（员工维护只在「员工管理」页） | 待实现 |
| 2.3 | 删除「台账数据」页「发票」「收款」Tab（发票→新发票台账页；收款查看/编辑另行安排） | 待实现 |
| 2.5 | `change_log` 仅记发票台账修改 | 待实现 |
| 1 | 数据清空功能（输入"我确认清空数据"验证） | 待另出规格 |

---

## 13. 待 UI 工程师 / 用户确认的开放问题

1. **编辑交互**：行内单元格编辑 vs 仅对话框编辑？（建议：对话框为主 + 可选行内编辑）
2. **同步粒度**：「同步到 invoice」默认同步"全部待同步行"还是"仅选中行"？（建议：默认全部待同步，提供"仅选中"开关）
3. **删除级联**：删 raw 已同步行时，是否默认级联删 `invoice`？（建议：默认级联 + 二次确认）
4. **多 sheet 展示**：用顶部下拉筛选 sheet，还是用 Tab 分页？（建议：顶部下拉，避免 Tab 过多）
5. **主表默认列**：是否默认隐藏 `src_file`/`src_sheet`/`src_row` 等溯源列（放 tooltip），只在需要时展开？（建议：默认显示 sheet+行号，文件隐藏）
6. **开票日期显示**：raw 表展示原始文本（如 2025/1/3）还是归一化后（2025-01-03）？（方案 D 主张**原始文本**，归一化只在同步时发生）——请确认。

---

> 本文档为方案 D 的设计交付物。UI 工程师据此出设计稿；设计稿确认后由 AI 工程师实现（含 `raw_invoice` 建表、导入双写、页面 CRUD、同步、change_log 收敛、2.1~2.5 联动）。
